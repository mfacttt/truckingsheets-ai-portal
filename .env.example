# Base URL of the Solei API gateway (shared identity-service auth).
# Defaults to the production gateway when unset — override for local backend testing.
VITE_GATEWAY_BASE=https://api.soleidispatch.com
